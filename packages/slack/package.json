{
  "name": "@microfox/slack",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "lint": "eslint .",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@slack/web-api": "^6.7.2"
  },
  "devDependencies": {
    "tsup": "^5.10.1",
    "typescript": "^4.5.2",
    "@microfox/eslint-config": "workspace:*",
    "@microfox/tsconfig": "workspace:*"
  }
} 